// Dashboard-specific widget sets and defaults
(function(){
  // Empty defaults: do not auto-add widgets unless explicitly requested
  const Sales = [];
  const Operations = [];
  const Branch = [];

  window.DashboardConfigs = { sales: Sales, operations: Operations, branch: Branch };
})();
